{{#each orderBasket.items as |item|}}
<div class="row">
  <div class="col-xs-8">
    {{item.quantity}}
    {{item.name}}
  </div>
  <div class="col-xs-2">
    {{item.price}}
  </div>
  <div class="col-xs-2">
<i {{action 'removeItem' item}}class="fa fa-times" aria-hidden="true"></i>
  </div>
</div>

{{/each}}
{{totalPrice}}
<button{{action 'makeOrder'}}>Place Order</button>
